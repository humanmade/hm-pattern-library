/**
 * ESLint Test File.
 *
 * All the bad code we want to catch!
 */

// Fail Semicolon
var x = 1;

// Space inside array brackets.
// Spaces after commas.
// Unused variable
var unused = [1,2]

// Yoda conditions are dumb and solve the wrong problem. - RM
// Space inside parens.
if(1 === x){
	alert(1)
}
